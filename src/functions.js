//@ts-check

/**
 * Add numbers
 * @param {number} n1 First Param 
 * @param {Number} n2 Second Param
 * @returns {number} total adition 
 */


function add(n1, n2) {
    return n1 + n2;
}

/**
 * Add Two numbers and return a String 
 * @param {number} n1 First Number 
 * @param {number} n2 Second Number 
 * @returns {String}
 */

const add2 = (n1, n2) => `The return this ${n1 + n2}`;