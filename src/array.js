//@ts-check

/**
 * Array of user's age
 * Arreglo de usuarios
 * @type {Array}
 */
const age = [16, 20 ,16 ,15];


/**
 * Array of points
 * @type {Array<Number>}
 */

const puntaje = [10, 199, 164];

/**
 * this is array of numbers and Strings, 
 * el uso de los simbolos sirve para agregar el tipo de datos.
 * @type {Array<Number | String>} 
 */

const scoresAndString = [10, "30", 12];

/**
 * Array of names
 * @type {Array<String>}
 */

const nombres = ["Erick", "Kenlly"];

nombres.push("loquesea");
